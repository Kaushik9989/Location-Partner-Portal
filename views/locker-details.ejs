<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Locker Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root { --primary: #6366f1; --bg: #f8fafc; --sidebar-width: 260px; }
        body { background-color: var(--bg); font-family: 'Inter', sans-serif; }
        #main-wrapper { margin-left: var(--sidebar-width); padding: 2rem; padding-top: 90px; }
        .glass-card { background: white; border-radius: 16px; border: 1px solid #e2e8f0; padding: 1.5rem; }
        .stat-label { font-size: 0.7rem; font-weight: 700; text-transform: uppercase; color: #64748b; letter-spacing: 0.05em; }
        .table thead th { background: #f8fafc; font-size: 0.75rem; text-transform: uppercase; color: #64748b; border-top: none; }
        .status-pill { padding: 0.4rem 0.8rem; border-radius: 50rem; font-size: 0.75rem; font-weight: 600; }
        @media (max-width: 992px) { #main-wrapper { margin-left: 0; } }
    </style>
</head>
<body>
    <%- include("./imports/topbar") %>
    <%- include("./imports/sidebar") %>

    <div id="main-wrapper">
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <a href="/partner/my-lockers" class="text-decoration-none small fw-bold text-primary"><i class="fas fa-chevron-left"></i> Back to Fleet</a>
                    <h2 class="fw-bold text-dark mt-2 mb-0">Locker: <%= locker.lockerId %></h2>
                </div>
                <div class="text-end">
                    <span class="text-muted small d-block">Last Synced: Just Now</span>
                    <span class="badge bg-success-subtle text-success border">Live Connectivity</span>
                </div>
            </div>

            <div class="row g-4 mb-4">
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label d-block mb-1">Store Clicks</span>
                        <h3 class="fw-bold mb-0 text-dark"><%= locker.stats.storeClicks %></h3>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label d-block mb-1">Send Clicks</span>
                        <h3 class="fw-bold mb-0 text-dark"><%= locker.stats.sendClicks %></h3>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label d-block mb-1">Drop Clicks</span>
                        <h3 class="fw-bold mb-0 text-dark"><%= locker.stats.dropClicks %></h3>
                    </div>
                </div>
            </div>

            <div class="glass-card">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">Compartment Real-Time Management</h5>
                    <button class="btn btn-sm btn-outline-secondary" onclick="window.location.reload()"><i class="fas fa-sync-alt"></i> Refresh</button>
                </div>

                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>Box ID</th>
                                <th>Hardware Status</th>
                                <th>Booking Status</th>
                                <th>Size</th>
                                <th>Parcel/User Info</th>
                                <th class="text-end">Overstay</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% locker.compartments.forEach(comp => { %>
                                <tr>
                                    <td class="fw-bold text-dark"><%= comp.compartmentId %></td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <i class="fas <%= comp.isLocked ? 'fa-lock text-muted' : 'fa-lock-open text-danger animate-pulse' %> me-2"></i>
                                            <span class="small fw-medium"><%= comp.isLocked ? 'Secure' : 'Open' %></span>
                                        </div>
                                    </td>
                                    <td>
                                        <% if (comp.currentParcelId) { %>
                                            <span class="status-pill bg-danger-subtle text-danger">Occupied</span>
                                        <% } else if (comp.isBooked) { %>
                                            <span class="status-pill bg-warning-subtle text-warning">Booked</span>
                                        <% } else { %>
                                            <span class="status-pill bg-success-subtle text-success">Available</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <span class="badge bg-light text-dark border text-capitalize"><%= comp.size %></span>
                                    </td>
                                    <td>
                                        <% if (comp.bookingInfo.recieverPhone) { %>
                                            <div class="small fw-bold text-dark"><%= comp.bookingInfo.recieverName %></div>
                                            <div class="extra-small text-muted"><%= comp.bookingInfo.recieverPhone %></div>
                                        <% } else if (comp.currentParcelId) { %>
                                            <code class="extra-small">PARCEL: <%= comp.currentParcelId %></code>
                                        <% } else { %>
                                            <span class="text-muted small">---</span>
                                        <% } %>
                                    </td>
                                    <td class="text-end">
                                        <% if (comp.isOverstay) { %>
                                            <span class="badge bg-danger rounded-pill" title="Parcel exceeds pickup time limit">
                                                <i class="fas fa-clock"></i> ALERT
                                            </span>
                                        <% } else { %>
                                            <span class="text-muted small">---</span>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>