<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Partner Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #6366f1;
            --bg: #f8fafc;
            --sidebar-width: 260px;
        }
        body { background-color: var(--bg); font-family: 'Inter', sans-serif; }
        #main-wrapper { margin-left: var(--sidebar-width); padding: 2rem; padding-top: 90px; }
        .glass-card { 
            background: white; 
            border-radius: 16px; 
            border: 1px solid #e2e8f0; 
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); 
            padding: 1.5rem; 
            transition: all 0.3s ease;
        }
        .glass-card:hover { transform: translateY(-5px); box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1); }
        .progress { background-color: #f1f5f9; border-radius: 10px; }
        .progress-bar { background-color: var(--primary); border-radius: 10px; }
        @media (max-width: 992px) { #main-wrapper { margin-left: 0; } }
    </style>
</head>
<body>
    <%- include("./imports/topbar") %>
    <%- include("./imports/sidebar") %>

    <div id="main-wrapper">
        <div class="container-fluid">
            <div class="mb-5">
                <h2 class="fw-bold text-dark mb-1">My Locker Fleet</h2>
                <p class="text-muted small">Overview of all hardware installed at your locations.</p>
            </div>

            <div class="row g-4">
                <% if (lockers && lockers.length > 0) { %>
                    <% lockers.forEach(locker => { %>
                        <div class="col-md-6 col-xl-4">
                            <div class="glass-card h-100 d-flex flex-column">
                                <div class="d-flex justify-content-between align-items-start mb-3">
                                    <div>
                                        <h5 class="fw-bold text-primary mb-0"><%= locker.lockerId %></h5>
                                        <span class="text-muted small"><i class="fas fa-barcode me-1"></i> Serialized Asset</span>
                                    </div>
                                    <span class="badge bg-success-subtle text-success">Online</span>
                                </div>
                                
                                <div class="mb-4">
                                    <p class="text-dark small mb-1 fw-medium"><i class="fas fa-map-marker-alt me-1 text-primary"></i> <%= locker.location.address %></p>
                                    <p class="text-muted extra-small ms-3"><%= locker.location.pincode %></p>
                                </div>

                                <% 
                                    let occupied = locker.compartments.filter(c => c.currentParcelId || c.isBooked).length;
                                    let total = locker.compartments.length;
                                    let percent = total > 0 ? (occupied / total) * 100 : 0;
                                %>
                                <div class="mt-auto">
                                    <div class="d-flex justify-content-between small mb-2">
                                        <span class="text-muted">Utilization</span>
                                        <span class="fw-bold text-dark"><%= occupied %> / <%= total %> Slots</span>
                                    </div>
                                    <div class="progress mb-4" style="height: 8px;">
                                        <div class="progress-bar" style="width: <%= percent %>%"></div>
                                    </div>
                                    <a href="/partner/my-lockers/<%= locker._id %>" class="btn btn-primary w-100 fw-bold">
                                        Open Management <i class="fas fa-arrow-right ms-2"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-12 text-center py-5">
                        <i class="fas fa-box-open fa-3x text-muted mb-3 opacity-25"></i>
                        <p class="text-muted">No lockers have been assigned to your account yet.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>