<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Host a Droppoint | Partner Application</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet"/>

  <style>
    :root {
      --primary-emerald: #059669;
      --hover-emerald: #047857;
      --dark-navy: #020617;
      --border-color: #1e293b;
    }

    body {
      font-family: "Plus Jakarta Sans", sans-serif;
      min-height: 100vh;
      background: radial-gradient(circle at center, #0f172a, #020617);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      padding: 20px;
    }

    .card-wrapper {
      width: 100%;
      max-width: 680px;
    }

    .access-card {
      background: #ffffff;
      border-radius: 24px;
      box-shadow: 0 40px 100px rgba(0,0,0,0.5);
      padding: 48px;
      color: #1e293b;
      position: relative;
      overflow: hidden;
    }

    /* Subtle top accent bar */
    .access-card::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 6px;
      background: var(--primary-emerald);
    }

    .logo {
      text-align: center;
      margin-bottom: 32px;
    }

    .logo img {
      height: 70px;
      filter: none; /* Ensure logo is full color on white bg */
    }

    .form-label {
      color: #475569;
      font-weight: 600;
      font-size: 0.9rem;
      margin-bottom: 8px;
    }

    .form-control, .form-select {
      border-radius: 12px;
      padding: 12px 16px;
      border: 2px solid #f1f5f9;
      background: #f8fafc;
      color: #1e293b;
      transition: all 0.2s ease;
    }

    .form-control:focus, .form-select:focus {
      background: #fff;
      border-color: var(--primary-emerald);
      box-shadow: 0 0 0 4px rgba(5, 150, 105, 0.1);
      outline: none;
    }

    .btn-primary {
      background: var(--primary-emerald);
      border: none;
      border-radius: 12px;
      padding: 14px;
      font-weight: 700;
      font-size: 1rem;
      letter-spacing: 0.3px;
      transition: transform 0.2s ease;
    }

    .btn-primary:hover {
      background: var(--hover-emerald);
      transform: translateY(-1px);
    }

    .section-title {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 20px;
      color: var(--primary-emerald);
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 800;
    }

    .section-title hr {
      flex-grow: 1;
      opacity: 0.1;
      margin: 0;
    }

    .back-link {
      color: #64748b;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.9rem;
      transition: color 0.2s;
    }

    .back-link:hover {
      color: var(--primary-emerald);
    }
  </style>
</head>
<body>
<div class="card-wrapper">
  <div class="access-card">
    
<% if (typeof flash !== 'undefined' && flash) { %>
  <div class="alert alert-<%= flash.type === 'error' ? 'danger' : 'success' %> border-0 shadow-sm mb-4" style="border-radius: 12px;">
    <i class="fas <%= flash.type === 'error' ? 'fa-exclamation-circle' : 'fa-check-circle' %> me-2"></i>
    <%= flash.message %>
  </div>
<% } %>
    <div class="logo">
    <img src="/images/droppointlogo.png" alt="Droppoint" style="background-color: #0f172a; border-radius: 12px; padding: 10px;">    </div>

    <div class="text-center mb-5">
      <h3 class="fw-bold mb-2">Let's Get Started</h3>
      <p class="text-muted">Tell us a bit about yourself and your space to join the network.</p>
    </div>

    <form method="POST" action="/request-access">
      
      <div class="section-title">
        <i class="fas fa-store"></i> Your Brand <hr>
      </div>

      <div class="row g-3 mb-4">
        <div class="col-md-6">
          <label class="form-label">Property/ Business Name</label>
          <input name="partnerName" class="form-control" placeholder="e.g. Blue Star Cafe" required>
        </div>
        <div class="col-md-6">
    <label class="form-label">Property Type</label>
    <div class="input-group">
        <span class="input-group-text bg-light border-2 border-end-0" style="border-radius: 12px 0 0 12px; border-color: #f1f5f9;">
            <i class="fas fa-building text-muted"></i>
        </span>
        <select name="propertyType" class="form-select border-start-0" style="border-radius: 0 12px 12px 0;" required>
            <option value="" disabled selected>Select property...</option>
            <option value="residential">Apartment / Society</option>
            <option value="retail">Retail Store / Mall</option>
            <option value="office">Corporate Office / Tech Park</option>
            <option value="university">University / Campus</option>
            <option value="transport">Metro / Fuel Station</option>
            <option value="other">Other Commercial</option>
        </select>
    </div>
</div>
        <div class="col-md-12">
            <label class="form-label">How would you like to earn?</label>
            <div class="input-group">
                <span class="input-group-text bg-light border-2 border-end-0" style="border-radius: 12px 0 0 12px; border-color: #f1f5f9;">
                    <i class="fas fa-hand-holding-usd text-muted"></i>
                </span>
                <select name="revenueModel" class="form-select border-start-0" style="border-radius: 0 12px 12px 0;" required>
                  <option value="" disabled selected>Choose a partner model...</option>
                  <option value="revenue_share">Revenue Share</option>
                  <option value="fixed_rent">Fixed Monthly Rent (Guaranteed income)</option>
                  <option value="franchise">Franchise (Own the equipment)</option>
                  <option value="hybrid">Hybrid (Rent + Revenue Share)</option>
                </select>
            </div>
          </div>
      </div>

      <div class="section-title">
        <i class="fas fa-user-circle"></i> Point of Contact <hr>
      </div>

      <div class="row g-3 mb-4">
        <div class="col-md-6">
          <label class="form-label">Your Name</label>
          <input name="contactPerson" class="form-control" placeholder="Full Name" required>
        </div>
        <div class="col-md-6">
          <label class="form-label">Email Address</label>
          <input name="email" type="email" class="form-control" placeholder="name@email.com" required>
        </div>
        <div class="col-md-12">
          <label class="form-label">Phone Number</label>
          <input name="phone" class="form-control" placeholder="10-digit mobile number" required>
        </div>
      </div>

      <div class="section-title">
        <i class="fas fa-map-marked-alt"></i> Where should we install? <hr>
      </div>

      <div class="mb-3">
        <label class="form-label">Site Address</label>
        <textarea name="address" class="form-control" rows="2" placeholder="Building name, street, and landmark..." required></textarea>
      </div>

      <div class="row g-3 mb-4">
        <div class="col-md-4">
          <label class="form-label">City</label>
          <input name="city" class="form-control" placeholder="e.g. Hyderabad" required>
        </div>
        <div class="col-md-4">
          <label class="form-label">State</label>
          <input name="state" class="form-control" placeholder="e.g. Telangana" required>
        </div>
        <div class="col-md-4">
          <label class="form-label">Pincode</label>
          <input name="pincode" class="form-control" placeholder="e.g. 500001" required>
        </div>
      </div>

      <button class="btn btn-primary w-100 shadow-lg py-3">
        Send My Application <i class="fas fa-paper-plane ms-2 small"></i>
      </button>

      <div class="text-center mt-4">
        <a href="/login" class="back-link">
          <i class="fas fa-arrow-left me-1"></i> I'm already a partner
        </a>
      </div>

    </form>
  </div>
  
  <p class="text-center mt-4 text-white-50" style="font-size: 0.85rem;">
    By sending this, you agree to our <a href="#" class="text-white text-decoration-none border-bottom">Partner Terms</a>.
  </p>
</div>

</body>
</html>